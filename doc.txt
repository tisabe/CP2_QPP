Hier die Liste für Arbeitsteilung:
einzelne Funktionen und Tests werden genau beschrieben und wer dafür zuständig ist

COORD2INDEX (Tim, work in progress)
  input: 
    coord: coordinates of a point						int array of size D
    N: length of all axis							unsigned int
    D: number of dimensions							unsigned int
  output:
    i: raveled index depending on coordinates					unsigned int
	Test: ? wip

INDEX2COORD (Tim, wip)
  input:
    index: index of array							unsigned int
    N: length of all axis							unsigned int
    D: number of dimensions							unsigned int
  output:
    coord: coordinates of a point						int array of size D
	Test: ? wip
		
NNEIGHBOUR (Lennart, wip)
	input:
		index: of a point i						unsigned int
		a: axis along which neighbour is computed			unsigned int: 0, 1 or 2
		p: direction in which neighbour is computed			int: -1 or +1
		N: length of all axes						unsigned int
		D: number of dimensions						unsigned int
	output:
		i: index of nearest neighbour on specified axis and direction	unsigned int
	Test: ? wip
	
LAPLACIAN (?, wip)
	input:
		psi: vector of discretized wavefunction				double array of size N**D
	output:
		lpsi: vector with laplacian of wave function			double array of size N**D
	Test: ? wip
	
KINETIC (?, wip)
	input:
	output:
	Test: ? wip
	
HARMONIC (?, wip)
	input:
	output:
	Test: ? wip


